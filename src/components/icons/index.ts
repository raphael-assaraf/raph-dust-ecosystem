/**
 * Barrel for all Sparkle "app" icons (172 icons), bulk-copied from
 * dust-main/sparkle/src/icons/app/. Use any of them as a normal React
 * component — they're SVGs that use `currentColor` so they tint with
 * Tailwind text-color utilities.
 *
 * Example: `<RocketIcon className="h-4 w-4 text-blue-500" />`
 */
export * from "./app";
